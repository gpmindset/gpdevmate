{
  "name": "@gpdevmate/core",
  "version": "1.0.0",
  "description": "GPdevmate's internal core library contains core logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -w",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gpmindset/gpdevmate.git"
  },
  "private": true,
  "dependencies": {
    "openai": "^5.8.2"
  }
}
